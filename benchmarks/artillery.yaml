config:
  target: 'http://localhost:3001'
  phases:
    - duration: 5
      arrivalRate: 10
      rampTo: 20
      name: "Warm up the application"
    - duration: 20
      arrivalRate: 20
      name: "Sustained max load"
scenarios:
  - flow:
    - get:
        url: "/"
